DATABASE_URL=postgresql://user:password@localhost:5432/fusonems
JWT_SECRET=replace-me
# Frontend: backend base URL including /api (e.g. http://localhost:8000/api) so /auth/login hits the API
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Getting platform access (development):
# 1. In backend/.env set ENV=development and LOCAL_AUTH_ENABLED=true
# 2. Start backend and frontend, open /login
# 3. Click "Dev access (founder) — no password" to create founder user (dev@local) and log in, or
# 4. Log in with dev@local / Pass1234 (you will be asked to change it on first login), or run: curl -X POST http://localhost:8000/api/auth/dev_seed

# Sole biller (you're the only biller): AI does as much as possible. See docs/SOLO_BILLER_SETUP.md and backend/.env for OLLAMA/Telnyx.

# Billing/support line (Telnyx number; shown on billing portal and patient bill pages)
NEXT_PUBLIC_BILLING_PHONE=+1 (715) 254-3027

# Stripe (patient payments, checkout): see docs/STRIPE_READY.md
# Backend: STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PUBLISHABLE_KEY in backend/.env
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
